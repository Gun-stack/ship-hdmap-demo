# M3b Unity 씬 UX 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 브라우저의 Unity 씬에서 마커를 클릭해 선택하고 드래그해 옮기면 DB 좌표가 바뀌며, 선택이 3D·트리·미니맵·HUD 에서 일치한다. 궤도 카메라, 차로·구획 라인, 갑판 라벨, UI Toolkit HUD 가 붙고 WebGL 이 재빌드된다.

**Architecture:** Unity 쪽은 기존 `MapRuntime`(GameObject `Map`)에 네 조각을 더한다 — `LandmarkPlacer` 의 선택·드래그 이벤트, `OrbitCamera`(Main Camera 에 런타임 부착), `MapOverlay`(Load 시 LineRenderer 생성), `HudView`(UI Toolkit, `LocalizationHud` 대체). DB 가 진실이므로 씬은 이동을 스스로 확정하지 않고 `onFeatureMoved` 로 알리기만 하며, 웹 스토어가 `PUT /features/{id}` 로 저장한다. 씬 우클릭 삭제는 제거한다(웹 폼만 삭제). 웹 쪽은 M3a 파킹 항목 정리(Task 1)와 `onFeatureMoved` 처리(Task 7)뿐이다.

**Tech Stack:** Unity 6000.3.24f1(Built-in RP, 레거시 Input, UI Toolkit `com.unity.modules.uielements`), NUnit EditMode; web: pnpm + Vite 8 + React 19 + TypeScript 6(`erasableSyntaxOnly`) + Zustand 5 + Vitest 5; API Spring Boot 4(M2, 변경 없음)

**Spec:** `docs/superpowers/specs/2026-09-15-ship-hdmap-demo-design.md` — §8 화면(중앙 HUD), §9.1 편집·저장, §10 브리지 계약, §13 M3b 행. Task 1 이 §9·§10 의 페이로드 세 줄을 갱신한다.

## Global Constraints

- 브리지 계약(스펙 §10, Task 1 갱신 후): R→U `Load`, `SetMode`, `SetDeck`, `Select`, `Confirm`, `Delete`, `SetPose`(M5), `SetNoise`, `StartScenario({mode})`; U→R `onSeedReady`, `onFeatureCreated{tempId, layer, x, y, z, deck, mounted_on}`, **`onFeatureMoved{id, x, y, z, normal[3], deck, mounted_on}`**, `onSelected{id}`, `onSlotFilled`(M5), `onLocalization`. 페이로드는 JSON 문자열, 좌표는 Ship Frame(x 선수+, y 좌현+, z 상방+), 각도는 도
- GameObject 이름 `Map`, 메서드명은 위 그대로. `Select(id)` 는 씬 하이라이트만 하고 `onSelected` 를 되돌려 보내지 않는다(에코 제거). 씬에서 마커를 클릭했을 때만 `onSelected`
- **DB 가 진실**: 씬은 삭제·이동을 확정하지 않는다. 드래그를 놓으면 `onFeatureMoved` 만 보내고 웹이 PUT 한다. 우클릭 삭제는 제거
- 마우스: 좌클릭 = 마커 선택·기둥 배치·마커 드래그, 우클릭 드래그 = 궤도 회전, 가운데 드래그 = 팬, 휠 = 줌
- WebGL 빌드 설정은 `unity/Assets/ShipHdMap/Editor/WebGLBuild.cs` 안에서만 바꾼다(압축 Disabled, `stripEngineCode=false`, `runInBackground=true`, Always Included Shaders: `Standard`, `Unlit/Texture`, **`Unlit/Color`(M3b 추가)**). 산출물 `web/public/unity/` 는 gitignore
- 좌표 변환은 `ShipFrame.ToUnity/ToShip` 만 쓴다(Unity(x, z, −y))
- 포트: API 8081, PostGIS 5433, Vite 5173(막혀 있으면 Vite 가 고른 포트 사용). 5432/8080 은 다른 프로젝트
- 저장소는 public. 기관·회사·과제명·이력 표현, 사용자 계정명이 든 절대경로, 실제 항구 좌표 금지. 사설 단어목록은 `SENSITIVE_TERMS_FILE` 환경변수로만 참조
- 커밋은 각 Task 끝에 한다. 푸시는 사용자 요청 시에만. Unity 배치 명령(테스트·빌드·에셋 생성)은 에디터 GUI 를 닫은 상태에서만 돌고, `-projectPath` 는 항상 `unity/`
- 테스트 목표: Unity EditMode 40 → 48, web Vitest 15 → 18, api 35 유지(변경 없음)

---

## 파일 구조

```
docs/superpowers/specs/2026-09-15-ship-hdmap-demo-design.md   (수정) §9.1·§9.3·§10 페이로드 3줄
docs/api-contract.md                                            (수정) 브리지 절
web/src/bridge/useShipUnity.ts        (수정) r.ok 검사; onFeatureMoved → store.moveFeature
web/src/components/DrivePanel.tsx     (수정) SetNoise 는 놓을 때, 정지는 setMode 만
web/src/components/PosePanel.tsx      (수정) 드래그 중 재동기화 보류
web/src/components/TopBar.tsx, DeckTabs.tsx, App.css   (수정) span → button
web/src/components/MiniMap.tsx        (수정) 전체 보기 = 면적 최대 갑판, 빈 윤곽 가드
web/src/components/PropertyForm.tsx   (수정) 초안 props 에 mounted_on 채움
web/src/store/editor.ts               (수정) 쓰기 후 version 재조회, moveFeature
web/src/api/types.ts                  (수정) FeatureCreatedEvt.mounted_on, FeatureMovedEvt
web/src/App.tsx                       (수정) 중앙 영역 contextmenu 차단
scripts/m3-dev.sh                     (수정) 트랩이 Vite 자식도 종료
unity/Assets/ShipHdMap/Runtime/Landmarks/LandmarkMarker.cs   (수정) MoveTo, SetHighlighted
unity/Assets/ShipHdMap/Runtime/Landmarks/LandmarkPlacer.cs   (수정) Selected/Moved 이벤트, 드래그, 우클릭 삭제 제거
unity/Assets/ShipHdMap/Runtime/Bridge/BridgeMessages.cs      (수정) mounted_on, FeatureMovedEvt
unity/Assets/ShipHdMap/Runtime/Bridge/MapRuntime.cs          (수정) Highlight, OnMarkerMoved, Orbit, Overlay, HudView
unity/Assets/ShipHdMap/Runtime/Vehicle/OrbitCamera.cs        (신규)
unity/Assets/ShipHdMap/Runtime/Ship/MapOverlay.cs            (신규) 차로·구획 LineRenderer
unity/Assets/ShipHdMap/Runtime/Vehicle/HudView.cs            (신규) UI Toolkit HUD + 갑판 라벨
unity/Assets/ShipHdMap/Runtime/Vehicle/LocalizationHud.cs    (삭제)
unity/Assets/ShipHdMap/Editor/HudAssets.cs                   (신규) PanelSettings·테마 생성 메뉴
unity/Assets/ShipHdMap/Resources/HudPanelSettings.asset, HudTheme.tss   (생성·커밋)
unity/Assets/ShipHdMap/Editor/WebGLBuild.cs                  (수정) Unlit/Color 포함
unity/Assets/ShipHdMap/Tests/EditMode/{MapRuntimeTests, LandmarkMarkerTests(신규), OrbitCameraTests(신규), HudViewTests(신규)}.cs
README.md                                                    (수정) 상태
```

---

### Task 1: 스펙·계약 갱신과 M3a 파킹 정리(web)

**Files:**
- Modify: `docs/superpowers/specs/2026-09-15-ship-hdmap-demo-design.md`, `docs/api-contract.md`
- Modify: `web/src/bridge/useShipUnity.ts`, `web/src/components/DrivePanel.tsx`, `web/src/components/PosePanel.tsx`, `web/src/components/TopBar.tsx`, `web/src/components/DeckTabs.tsx`, `web/src/components/MiniMap.tsx`, `web/src/App.css`, `web/src/store/editor.ts`, `scripts/m3-dev.sh`
- Test: `web/src/store/editor.test.ts` (1개 추가)

**Interfaces:**
- 스펙 §10 세 줄이 Global Constraints 의 페이로드와 같아진다. 스펙 §9.1 2번 `onFeatureCreated{…, deck, mounted_on}`, §9.3 1번 `StartScenario({mode:"load"})`
- 스토어: 피처 생성·수정·삭제 뒤 `GET /datasets/{id}` 로 `version` 을 재조회해 `bumpVersion` 한다(실패는 무시)
- 스토어·컴포넌트 시그니처는 바뀌지 않는다(Task 7 이 `moveFeature` 를 더한다)

- [ ] **Step 1: 실패하는 테스트** — `web/src/store/editor.test.ts` 의 `describe` 안 끝에 추가:

```ts
  it("writes refresh the dataset version", async () => {
    await useEditorStore.getState().load("ds1");
    expect(useEditorStore.getState().dataset?.version).toBe(3);
    vi.mocked(api.getDataset).mockResolvedValueOnce({ id: "ds1", name: "d", version: 4, ap_lat: 0, ap_lon: 0, heading_deg: 0, lpp_m: 120 });
    await useEditorStore.getState().updateFeature("LM-0001", { props: { code: 9 } });
    expect(useEditorStore.getState().dataset?.version).toBe(4);
  });
```

- [ ] **Step 2: 실패 확인** — `cd web && pnpm test` → 이 테스트만 실패(`expected 3 to be 4`).

- [ ] **Step 3: 스토어** — `web/src/store/editor.ts`: `create<EditorState>()((set, get) => ({` 바로 위에 헬퍼를 두고, 세 액션 끝에서 부른다.

```ts
/** After any write the server bumped dataset.version; fetch it so the top bar stays honest. Failure is not an error worth showing. */
async function refreshVersion(get: () => EditorState) {
  try { const d = await api.getDataset(get().datasetId); get().bumpVersion(d.version); } catch { /* keep the old value */ }
}
```

`applyDraft` 의 `return { tempId, id: created.id };` 앞에 `await refreshVersion(get);`, `updateFeature` 의 `set(...)` 뒤에 `await refreshVersion(get);`, `removeFeature` 의 `set(...)` 뒤에 `await refreshVersion(get);`.

- [ ] **Step 4: 통과 확인** — `pnpm test` → 16/16.

- [ ] **Step 5: 훅 r.ok** — `web/src/bridge/useShipUnity.ts` 의 `fetch(api.vehicleMapUrl(datasetId)).then((r) => r.text())` 를

```ts
    fetch(api.vehicleMapUrl(datasetId)).then((r) => { if (!r.ok) throw new Error("vehicle-map HTTP " + r.status); return r.text(); })
```
로 바꾼다(다음 `.then`/`.catch`/`.finally` 는 그대로).

- [ ] **Step 6: DrivePanel** — 소음 슬라이더는 값은 바로 반영하되 Unity 전송은 놓을 때만, `정지` 는 `setMode("edit")` 만(모드 이펙트가 `SetMode` 를 보낸다). `web/src/components/DrivePanel.tsx` 전체:

```tsx
import { useState } from "react";
import { wrapDeg } from "../geo/shipFrame";
import { useEditorStore } from "../store/editor";
import type { BridgeName } from "../bridge/useShipUnity";

type Send = (name: BridgeName, payload?: string | object) => void;
type Sig = { sigma_r: number; sigma_theta: number; sigma_alpha: number };
const SLIDERS: { key: keyof Sig; label: string; max: number; step: number; digits: number }[] = [
  { key: "sigma_r", label: "σ 거리 (m)", max: 1, step: 0.05, digits: 2 },
  { key: "sigma_theta", label: "σ 방위 (°)", max: 5, step: 0.5, digits: 1 },
  { key: "sigma_alpha", label: "σ 방향각 (°)", max: 10, step: 0.5, digits: 1 },
];

export function DrivePanel({ send }: { send: Send }) {
  const { localization: l, setMode } = useEditorStore();
  const [sig, setSig] = useState<Sig>({ sigma_r: 0.2, sigma_theta: 1, sigma_alpha: 2 });
  const commit = () => send("SetNoise", { ...sig, sigma_gps: 0.5 }); // on release only — Unity's SetNoise is cheap but the bridge is not a slider event bus
  const err = l ? Math.hypot(l.est_x - l.true_x, l.est_y - l.true_y) : null;
  return (
    <div className="panel">
      <h4>주행 시뮬레이션</h4>
      <div className="row">
        <button className="btn primary" onClick={() => send("StartScenario", { mode: "load" })}>▶ 선적 시나리오</button>
        <button className="btn" onClick={() => setMode("edit")}>정지</button>
      </div>
      {SLIDERS.map((s) => (
        <div className="row" key={s.key}><label>{s.label}</label>
          <input type="range" min={0} max={s.max} step={s.step} value={sig[s.key]} onChange={(e) => setSig({ ...sig, [s.key]: Number(e.target.value) })}
            onMouseUp={commit} onKeyUp={commit} onTouchEnd={commit} />
          <span>{sig[s.key].toFixed(s.digits)}</span></div>
      ))}
      <h4 style={{ marginTop: 8 }}>위치 추정</h4>
      {!l ? <span style={{ color: "#888" }}>추정 없음</span> : (
        <div style={{ fontFamily: "monospace", fontSize: 12 }}>
          <div>frame {l.frame} · N {l.n_obs} · RMS {l.residual_rms.toFixed(3)}</div>
          <div>est  x {l.est_x.toFixed(2)} y {l.est_y.toFixed(2)} ψ {l.est_psi.toFixed(1)}</div>
          <div>true x {l.true_x.toFixed(2)} y {l.true_y.toFixed(2)} ψ {l.true_psi.toFixed(1)}</div>
          <div>err {err!.toFixed(2)} m · {wrapDeg(l.est_psi - l.true_psi).toFixed(1)}°</div>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 7: PosePanel** — 드래그 중에는 서버 응답이 로컬 값을 덮지 않게 한다. `web/src/components/PosePanel.tsx` 에서 `import { useEffect, useState } from "react";` → `import { useEffect, useRef, useState } from "react";`, `const [err, setErr] = ...` 다음 줄에 `const dragging = useRef(false);`, 이펙트를 `useEffect(() => { if (pose && !dragging.current) setLocal(pose); }, [pose]);` 로, `commit` 첫 줄에 `dragging.current = false;` 를 넣고, `<input type="range" …` 에 `onMouseDown={() => { dragging.current = true; }} onTouchStart={() => { dragging.current = true; }}` 를 추가한다.

- [ ] **Step 8: 탭을 버튼으로** — `TopBar.tsx` 의 두 `<span className={"tab" …} onClick=…>편집</span>` 을 `<button type="button" className={"tab" + (mode === "edit" ? " on" : "")} onClick={() => setMode("edit")}>편집</button>`(주행도 같은 꼴)으로, `DeckTabs.tsx` 의 `<span key={id} className=… onClick=…>` 을 `<button type="button" key={id} className=… onClick=…>` 으로 바꾼다. `App.css`: `.topbar .tab { … }` 줄을 `.topbar .tab { padding: 3px 10px; border-radius: 4px; background: #444; color: inherit; border: 0; font: inherit; cursor: pointer; }` 로, `.decktabs span` 두 규칙의 선택자를 `.decktabs button` 으로 바꾸고 첫 규칙에 `font: inherit; background: #fff; color: inherit;` 를 더한다.

- [ ] **Step 9: 미니맵 전체 보기** — `MiniMap.tsx` 에서 `const deck = decks[0] ?? s.decks[0];` 를

```ts
  const area = (d: { outline: number[][] }) => (d.outline.length ? bbox(d.outline).w * bbox(d.outline).h : 0);
  const deck = [...decks, ...s.decks].filter((d) => d.outline.length > 0).sort((a, b) => area(b) - area(a))[0];
```
로 바꾼다(전체면 가장 큰 갑판, 갑판 필터면 그 갑판; 빈 윤곽은 제외).

- [ ] **Step 10: 스크립트 트랩** — `scripts/m3-dev.sh` 마지막 줄 `cd "$ROOT/web" && pnpm install --frozen-lockfile >/dev/null && pnpm dev` 를

```bash
cd "$ROOT/web" && pnpm install --frozen-lockfile >/dev/null
pnpm dev & VITE_PID=$!
trap 'kill $VITE_PID $BOOT_PID 2>/dev/null || true; pkill -f "com.shiphdmap.api.ApiApplication" 2>/dev/null || true' EXIT
wait $VITE_PID
```
로 바꾼다(기존 `trap … EXIT` 줄은 그대로 두어도 되지만 이 새 trap 이 덮어쓴다). `bash -n scripts/m3-dev.sh`.

- [ ] **Step 11: 스펙·계약** — 스펙 §10 표에서
  - `| R→U | \`StartScenario\` | map, pose, \`"load"\` / \`"unload"\` |` → `| R→U | \`StartScenario\` | \`{mode: "load" | "unload"}\` — 맵은 이미 \`Load\` 된 것을 쓴다. pose 는 M5 에서 추가 |`
  - `| U→R | \`onFeatureCreated\` | tempId, layer, x, y, z, deck |` → `| U→R | \`onFeatureCreated\` | tempId, layer, x, y, z, deck, mounted_on |`
  - `| U→R | \`onFeatureMoved\` | id, x, y, z |` → `| U→R | \`onFeatureMoved\` | id, x, y, z, normal[3], deck, mounted_on — 드래그를 놓았을 때. 웹이 PUT 으로 확정 |`
  - §9.1 2번의 `onFeatureCreated{tempId, layer, x, y, z, deck}` → `onFeatureCreated{tempId, layer, x, y, z, deck, mounted_on}`; 3번 뒤에 `4. 3D 드래그 → 놓으면 Unity \`onFeatureMoved{id, x, y, z, normal, deck, mounted_on}\` → React 가 PUT → 응답으로 스토어 갱신` 을 넣고 기존 4번은 5번으로
  - §9.3 1번 `Unity \`StartScenario(map, pose, "load")\`` → `Unity \`StartScenario({mode:"load"})\`(pose 는 M5)`

  `docs/api-contract.md` 브리지 절의 U→R 줄을 `onSeedReady`, `onFeatureCreated{tempId,layer,x,y,z,deck,mounted_on}`, `onFeatureMoved{id,x,y,z,normal,deck,mounted_on}`, `onSelected{id}`, `onLocalization{…}` 로 갱신하고, R→U 의 `Select(id)` 뒤에 `(씬 하이라이트만; 에코 없음)` 을 붙인다.

- [ ] **Step 12: 검증과 커밋** — `cd web && pnpm test && pnpm build`(16/16, 클린), `bash -n scripts/m3-dev.sh`.

```bash
git add docs/superpowers/specs/2026-09-15-ship-hdmap-demo-design.md docs/api-contract.md web/src scripts/m3-dev.sh
git commit -m "web, docs: M3a leftovers — version refresh, release-gated noise, button tabs, minimap largest deck; spec payloads for M3b"
```

---

### Task 2: Unity — 씬 선택과 하이라이트, `mounted_on` 전달

**Files:**
- Modify: `unity/Assets/ShipHdMap/Runtime/Landmarks/LandmarkMarker.cs`, `unity/Assets/ShipHdMap/Runtime/Landmarks/LandmarkPlacer.cs`, `unity/Assets/ShipHdMap/Runtime/Bridge/BridgeMessages.cs`, `unity/Assets/ShipHdMap/Runtime/Bridge/MapRuntime.cs`
- Test: `unity/Assets/ShipHdMap/Tests/EditMode/MapRuntimeTests.cs`

**Interfaces:**
- `LandmarkMarker.SetHighlighted(bool)`: 자식 `Halo`(노란 Quad, 1.6배, 표면 쪽 4 mm)를 켜고 끈다. 콜라이더 없음
- `LandmarkPlacer.Selected` 이벤트(`Action<LandmarkMarker>`): 좌클릭이 `Landmark` 레이어의 마커를 맞히면 배치 대신 발생. 우클릭 삭제(`TryDelete`, `Deleted` 이벤트)는 제거
- `MapRuntime.Highlight(string id)`: 이전 하이라이트 해제, 있으면 켬, 없으면 해제만. `Select(id)` = `Highlight(id)`(에코 없음). `Selected` → `Highlight` + `onSelected` 전송. `Confirm` 은 선택 id 도 갱신, `Delete`/`Load` 는 선택 해제
- `FeatureCreatedEvt.mounted_on` 추가(배치 시 `hit.collider.name`)

- [ ] **Step 1: 실패하는 테스트** — `MapRuntimeTests.cs` 에 추가:

```csharp
        [Test]
        public void SelectHighlightsWithoutEmitting()
        {
            go = new GameObject("Map"); var rt = go.AddComponent<MapRuntime>(); rt.InitForTest(); rt.Load(Fixture());
            int emitted = 0; rt.Emit += (_, _) => emitted++;
            rt.Select("LM-0003");
            var halo3 = rt.LandmarksRoot.Find("LM-0003/Halo");
            Assert.That(halo3, Is.Not.Null); Assert.That(halo3.gameObject.activeSelf, Is.True);
            rt.Select("LM-0004");
            Assert.That(halo3.gameObject.activeSelf, Is.False);
            Assert.That(rt.LandmarksRoot.Find("LM-0004/Halo").gameObject.activeSelf, Is.True);
            rt.Select("LM-NOPE");
            Assert.That(rt.LandmarksRoot.Find("LM-0004/Halo").gameObject.activeSelf, Is.False);
            Assert.That(emitted, Is.EqualTo(0));
        }

        [Test]
        public void FeatureCreatedEventCarriesMountedOn()
        {
            var json = MapJson.Serialize(new FeatureCreatedEvt { tempId = "LM-0002", layer = "LM", x = 1, y = 2, z = 3, deck = "D3", mounted_on = "C-PILLAR-D3-001" });
            Assert.That(json, Does.Contain("mounted_on")); Assert.That(json, Does.Contain("C-PILLAR-D3-001"));
        }
```

- [ ] **Step 2: 실패 확인** — 에디터 GUI 를 닫고

```bash
U=/Applications/Unity/Hub/Editor/6000.3.24f1/Unity.app/Contents/MacOS/Unity
cd unity && "$U" -batchmode -nographics -projectPath "$PWD" -runTests -testPlatform EditMode -testResults "$PWD/Logs/editmode-results.xml" -logFile "$PWD/Logs/editmode.log"; echo EXIT=$?
grep -E "error CS" Logs/editmode.log | head -3
```
Expected: 컴파일 오류 `CS0117 'FeatureCreatedEvt' does not contain a definition for 'mounted_on'`(RED).

- [ ] **Step 3: BridgeMessages** — `FeatureCreatedEvt` 를 `public class FeatureCreatedEvt { public string tempId; public string layer; public double x, y, z; public string deck; public string mounted_on; }` 로.

- [ ] **Step 4: LandmarkMarker 하이라이트** — 클래스 안에 추가:

```csharp
        Transform _halo;

        /// Yellow frame behind the tag (4 mm toward the surface, 1.6x) so the selection reads at any camera distance.
        public void SetHighlighted(bool on)
        {
            if (_halo == null)
            {
                if (!on) return;
                var h = GameObject.CreatePrimitive(PrimitiveType.Quad); h.name = "Halo"; h.layer = gameObject.layer;
                Object.DestroyImmediate(h.GetComponent<Collider>());
                h.transform.SetParent(transform, false);
                h.transform.localPosition = new Vector3(0, 0, 0.004f); // marker forward is -normal, so +z is toward the surface
                h.transform.localScale = new Vector3(1.6f, 1.6f, 1f);
                h.GetComponent<Renderer>().sharedMaterial = new Material(Shader.Find("Unlit/Color")) { color = new Color(1f, 0.85f, 0.1f) };
                _halo = h.transform;
            }
            _halo.gameObject.SetActive(on);
        }
```

- [ ] **Step 5: LandmarkPlacer 선택 이벤트, 우클릭 삭제 제거** — 파일 전체:

```csharp
using System;
using System.Collections.Generic;
using UnityEngine;

namespace ShipHdMap
{
    /// Play-mode mouse: left click on a marker selects it, left click on ship structure spawns a marker facing the surface normal.
    /// Deletion happens only from the web form (DB first, then Delete(id)), so there is no right-click delete.
    public class LandmarkPlacer : MonoBehaviour
    {
        public Camera cam; public Transform landmarksRoot; public float sizeM = 0.3f;
        public int nextCode = 1; public bool enabledForInput = true;
        public int nextId = 1;
        public string NextId() => $"LM-{nextId++:0000}";
        public List<LandmarkMarker> All = new();
        public List<Deck> decks = new();   // for deckId lookup by height
        public event Action<LandmarkMarker> Created; public event Action<LandmarkMarker> Selected;

        void Update()
        {
            if (!enabledForInput || cam == null) return;
            if (Input.GetMouseButtonDown(0)) OnLeftDown();
        }

        public LandmarkMarker PlaceAt(RaycastHit hit)
        {
            string id = NextId();
            int code = nextCode++ % AprilTag36h11.Count;
            var lm = LandmarkMarker.Spawn(landmarksRoot, id, code, hit.point, hit.normal, sizeM, DeckIdForHeight(hit.point.y), hit.collider.name);
            All.Add(lm); Created?.Invoke(lm); return lm;
        }

        public static int StructureMask() { int mask = LayerMask.GetMask("ShipStructure"); return mask == 0 ? ~LayerMask.GetMask("Landmark") : mask; }

        void OnLeftDown()
        {
            var ray = cam.ScreenPointToRay(Input.mousePosition);
            int lmMask = LayerMask.GetMask("Landmark");
            if (lmMask != 0 && Physics.Raycast(ray, out var mh, 500f, lmMask) && mh.collider.TryGetComponent<LandmarkMarker>(out var lm)) { Selected?.Invoke(lm); return; }
            if (Physics.Raycast(ray, out var hit, 500f, StructureMask())) PlaceAt(hit);
        }

        public string DeckIdForHeight(float unityY)
        {
            string best = "D1"; double bestD = double.MaxValue;
            foreach (var d in decks) { double dd = Math.Abs(unityY - d.z_surface); if (unityY + 0.5 >= d.z_surface && dd < bestD) { bestD = dd; best = d.id; } }
            return best;
        }
    }
}
```

- [ ] **Step 6: MapRuntime** — (a) 필드 `string _selected;` 를 `string _mode = "edit";` 줄에 추가. (b) `Awake` 의 `Placer.Deleted += …;` 줄을 지우고 `Placer.Selected += lm => { Highlight(lm.id); Send(BridgeMessages.OnSelected, "{\"id\":\"" + lm.id + "\"}"); };` 로 바꾼다. `Placer.Created` 핸들러의 `FeatureCreatedEvt { … deck = lm.deckId }` 에 `, mounted_on = lm.mountedOn` 을 더한다. (c) `Select` 를 `public void Select(string id) => Highlight(id);` 로 바꾸고 아래를 추가:

```csharp
        /// Scene-side selection: at most one halo. Does not emit — the web already knows what it selected.
        public void Highlight(string id)
        {
            if (_selected != null && _markers.TryGetValue(_selected, out var prev) && prev) prev.SetHighlighted(false);
            _selected = id != null && _markers.ContainsKey(id) ? id : null;
            if (_selected != null) _markers[_selected].SetHighlighted(true);
        }
```
(d) `Confirm` 안 `_markers[c.id] = m;` 뒤에 `if (_selected == c.tempId) _selected = c.id;`. (e) `Delete` 의 `_markers.Remove(id);` 앞에 `if (_selected == id) _selected = null;`. (f) `Load` 의 `_markers.Clear();` 앞에 `_selected = null;`.

- [ ] **Step 7: 통과 확인** — Step 2 명령 → `total="42" passed="42"`.

- [ ] **Step 8: 커밋**

```bash
git add unity/Assets/ShipHdMap
git commit -m "unity: click-select with halo highlight, Select without echo, mounted_on in onFeatureCreated; drop right-click delete"
```

---

### Task 3: Unity — 마커 드래그 이동과 `onFeatureMoved`

**Files:**
- Modify: `LandmarkMarker.cs`, `LandmarkPlacer.cs`, `BridgeMessages.cs`, `MapRuntime.cs`
- Test: `unity/Assets/ShipHdMap/Tests/EditMode/LandmarkMarkerTests.cs`(신규), `MapRuntimeTests.cs`

**Interfaces:**
- `LandmarkMarker.MoveTo(Vector3 unityPos, Vector3 unityNormal, string deckId, string mountedOn)`: `Spawn` 과 같은 위치·회전 규칙. `Spawn` 은 이제 `MoveTo` 를 호출
- `LandmarkPlacer.Moved` 이벤트(`Action<LandmarkMarker>`): 마커를 좌클릭한 채 끌면 `ShipStructure` 표면을 따라 `MoveTo`; 놓았을 때 1 cm 이상 움직였으면 발생
- `MapRuntime.OnMarkerMoved(LandmarkMarker)`: `MapRefs` 갱신 + `onFeatureMoved{id, x, y, z, normal, deck, mounted_on}` 전송. `Awake` 가 `Placer.Moved += OnMarkerMoved`
- `FeatureMovedEvt { string id; double x, y, z; double[] normal; string deck; string mounted_on; }`

- [ ] **Step 1: 실패하는 테스트** — 신규 `LandmarkMarkerTests.cs`:

```csharp
using NUnit.Framework;
using UnityEngine;

namespace ShipHdMap.Tests
{
    public class LandmarkMarkerTests
    {
        GameObject go;
        [TearDown] public void Cleanup() { if (go) Object.DestroyImmediate(go); }

        [Test]
        public void MoveToRepositionsAndReorients()
        {
            go = new GameObject("root");
            var lm = LandmarkMarker.Spawn(go.transform, "LM-T", 1, ShipFrame.ToUnity(10, -6, 11), ShipFrame.ToUnity(0, 1, 0), 0.3f, "D3", "P1");
            lm.MoveTo(ShipFrame.ToUnity(20, 6, 11), ShipFrame.ToUnity(0, -1, 0), "D3", "P2");
            var m = lm.ToModel();
            Assert.That(m.position[0], Is.EqualTo(20).Within(1e-3));
            Assert.That(m.position[1], Is.EqualTo(6).Within(1e-3));
            Assert.That(m.position[2], Is.EqualTo(11).Within(1e-3));
            Assert.That(m.normal[1], Is.EqualTo(-1).Within(1e-3));
            Assert.That(m.mounted_on, Is.EqualTo("P2"));
        }
    }
}
```

`MapRuntimeTests.cs` 에 추가:

```csharp
        [Test]
        public void MarkerMovedUpdatesMapRefAndEmits()
        {
            go = new GameObject("Map"); var rt = go.AddComponent<MapRuntime>(); rt.InitForTest(); rt.Load(Fixture());
            string name = null, json = null; rt.Emit += (n, j) => { name = n; json = j; };
            var lm = rt.LandmarksRoot.Find("LM-0001").GetComponent<LandmarkMarker>();
            lm.MoveTo(ShipFrame.ToUnity(30, 6, 11.8), ShipFrame.ToUnity(0, -1, 0), "D3", "C-PILLAR-D3-009");
            rt.OnMarkerMoved(lm);
            Assert.That(rt.MapRefs["LM-0001"].mx, Is.EqualTo(30).Within(1e-3));
            Assert.That(rt.MapRefs["LM-0001"].phiRad, Is.EqualTo(-System.Math.PI / 2).Within(1e-3));
            Assert.That(name, Is.EqualTo("onFeatureMoved"));
            Assert.That(json, Does.Contain("C-PILLAR-D3-009")); Assert.That(json, Does.Contain("normal"));
        }
```

- [ ] **Step 2: 실패 확인** — EditMode 배치 실행 → `error CS1061 … 'MoveTo'`(RED).

- [ ] **Step 3: LandmarkMarker.MoveTo** — `Spawn` 의 `g.transform.position = …;` 와 `g.transform.rotation = …;` 두 줄을 지우고, `var lm = g.AddComponent<LandmarkMarker>();` 뒤의 필드 대입 줄을 `lm.id = id; lm.code = code; lm.sizeM = sizeM; lm.MoveTo(unityPos, unityNormal, deckId, mountedOn);` 로 바꾼다. 클래스에 추가:

```csharp
        /// Puts the tag 1 cm off the surface at unityPos, facing along unityNormal (Quad renders on -Z, so look at -normal).
        public void MoveTo(Vector3 unityPos, Vector3 unityNormal, string deckId, string mountedOn)
        {
            var n = unityNormal.normalized;
            transform.position = unityPos + n * 0.01f;
            transform.rotation = Quaternion.LookRotation(-n, Mathf.Abs(n.y) > 0.9f ? Vector3.forward : Vector3.up);
            this.deckId = deckId; this.mountedOn = mountedOn;
        }
```
(`Spawn` 의 `var n = unityNormal.normalized;` 줄은 이제 불필요하니 삭제.)

- [ ] **Step 4: Placer 드래그** — 필드에 `public event Action<LandmarkMarker> Moved;` 와 `LandmarkMarker _drag; Vector3 _dragStart; bool _moved;` 를 추가. `Update` 를

```csharp
        void Update()
        {
            if (!enabledForInput || cam == null) { _drag = null; return; }
            if (Input.GetMouseButtonDown(0)) OnLeftDown();
            if (_drag && Input.GetMouseButton(0)) DragTo();
            if (_drag && Input.GetMouseButtonUp(0)) EndDrag();
        }
```
로, `OnLeftDown` 의 마커 분기를 `{ _drag = lm; _dragStart = lm.transform.position; _moved = false; Selected?.Invoke(lm); return; }` 로 바꾸고 추가:

```csharp
        void DragTo()
        {
            if (!Physics.Raycast(cam.ScreenPointToRay(Input.mousePosition), out var hit, 500f, StructureMask())) return; // marker colliders are on Landmark, so they never block the drag ray
            _drag.MoveTo(hit.point, hit.normal, DeckIdForHeight(hit.point.y), hit.collider.name);
            if ((_drag.transform.position - _dragStart).sqrMagnitude > 1e-4f) _moved = true;
        }

        void EndDrag() { if (_moved) Moved?.Invoke(_drag); _drag = null; _moved = false; }
```

- [ ] **Step 5: BridgeMessages + MapRuntime** — `BridgeMessages.cs` 에 `public class FeatureMovedEvt { public string id; public double x, y, z; public double[] normal; public string deck; public string mounted_on; }`. `MapRuntime.Awake` 의 `Placer.Selected += …;` 다음에 `Placer.Moved += OnMarkerMoved;`. 클래스에 추가:

```csharp
        /// Drag ended in the scene: refresh the localization map entry and tell the web, which persists it (PUT) — the scene never commits a move itself.
        public void OnMarkerMoved(LandmarkMarker lm)
        {
            var m = lm.ToModel(); MapRefs[lm.id] = RefOf(m);
            Send(BridgeMessages.OnFeatureMoved, MapJson.Serialize(new FeatureMovedEvt { id = lm.id, x = m.position[0], y = m.position[1], z = m.position[2], normal = m.normal, deck = lm.deckId, mounted_on = lm.mountedOn }));
        }
```

- [ ] **Step 6: 통과 확인** — EditMode 배치 실행 → `total="44" passed="44"`.

- [ ] **Step 7: 커밋** — `git add unity/Assets/ShipHdMap && git commit -m "unity: drag markers along ship structure, emit onFeatureMoved on release"`

---

### Task 4: Unity — 궤도 카메라와 차량 추적

**Files:**
- Create: `unity/Assets/ShipHdMap/Runtime/Vehicle/OrbitCamera.cs`
- Modify: `MapRuntime.cs`
- Test: `unity/Assets/ShipHdMap/Tests/EditMode/OrbitCameraTests.cs`(신규)

**Interfaces:**
- `OrbitCamera`(MonoBehaviour, Main Camera): `target`, `yawDeg`, `pitchDeg`, `distance`, `follow`(Transform, 있으면 target 이 따라감), `inputEnabled`. `static (Vector3 pos, Quaternion rot) Pose(target, yawDeg, pitchDeg, distance)`, `Focus(Vector3 p, float dist)`(follow 해제)
- 입력: 우클릭 드래그 회전(`Mouse X/Y`), 가운데 드래그 팬, 휠 줌(0.9^틱). `LateUpdate` 에서 적용
- `MapRuntime.Orbit`: `Awake` 에서 `cam` 에 없으면 부착. `Highlight` 가 선택 마커로 `Focus(pos, 12)`, `StartScenario` 가 `follow = Vehicle.transform`, `SetMode("edit")` 가 `follow = null`

- [ ] **Step 1: 실패하는 테스트** — `OrbitCameraTests.cs`:

```csharp
using NUnit.Framework;
using UnityEngine;

namespace ShipHdMap.Tests
{
    public class OrbitCameraTests
    {
        [Test]
        public void PosePutsCameraBehindTargetLookingAtIt()
        {
            var (p, r) = OrbitCamera.Pose(new Vector3(60, 10, 0), 0, 0, 10);
            Assert.That(p.x, Is.EqualTo(60).Within(1e-4)); Assert.That(p.y, Is.EqualTo(10).Within(1e-4)); Assert.That(p.z, Is.EqualTo(-10).Within(1e-4));
            Assert.That((r * Vector3.forward).z, Is.EqualTo(1).Within(1e-4));          // looks +z toward the target
            var (top, _) = OrbitCamera.Pose(Vector3.zero, 0, 90, 5);
            Assert.That(top.y, Is.EqualTo(5).Within(1e-4));                                // pitch 90 = straight above
            var (side, _) = OrbitCamera.Pose(Vector3.zero, 90, 0, 5);
            Assert.That(side.x, Is.EqualTo(-5).Within(1e-4));                              // yaw 90 = camera on -x looking +x
        }
    }
}
```

- [ ] **Step 2: 실패 확인** — EditMode 배치 → `CS0246 'OrbitCamera' could not be found`.

- [ ] **Step 3: OrbitCamera.cs**

```csharp
using UnityEngine;

namespace ShipHdMap
{
    /// Orbit camera on the Main Camera: right-drag orbits, middle-drag pans, wheel zooms. Left button is left to LandmarkPlacer.
    /// Set `follow` to track a transform (drive mode); Focus() jumps to a point and stops following.
    public class OrbitCamera : MonoBehaviour
    {
        public Vector3 target = new Vector3(60, 10, 0);
        public float yawDeg = -20f, pitchDeg = 25f, distance = 70f;
        public float minDistance = 3f, maxDistance = 300f, orbitSpeed = 3f, panSpeed = 0.02f;
        public Transform follow; public float followLerp = 4f;
        public bool inputEnabled = true;

        /// Camera position/rotation for a target, yaw about world up (deg), pitch above the horizon (deg) and distance.
        public static (Vector3 pos, Quaternion rot) Pose(Vector3 target, float yawDeg, float pitchDeg, float distance)
        {
            var rot = Quaternion.Euler(pitchDeg, yawDeg, 0);
            return (target - rot * Vector3.forward * distance, rot);
        }

        public void Focus(Vector3 p, float dist) { follow = null; target = p; distance = Mathf.Clamp(dist, minDistance, maxDistance); Apply(); }

        void LateUpdate()
        {
            if (inputEnabled)
            {
                if (Input.GetMouseButton(1)) { yawDeg += Input.GetAxis("Mouse X") * orbitSpeed; pitchDeg = Mathf.Clamp(pitchDeg - Input.GetAxis("Mouse Y") * orbitSpeed, -5f, 89f); }
                if (Input.GetMouseButton(2))
                {
                    var flat = Quaternion.Euler(0, yawDeg, 0);
                    target -= flat * new Vector3(Input.GetAxis("Mouse X"), 0, Input.GetAxis("Mouse Y")) * (panSpeed * distance);
                    follow = null;
                }
                float wheel = Input.mouseScrollDelta.y;
                if (wheel != 0f) distance = Mathf.Clamp(distance * Mathf.Pow(0.9f, wheel), minDistance, maxDistance);
            }
            if (follow) target = Vector3.Lerp(target, follow.position, 1f - Mathf.Exp(-followLerp * Time.deltaTime));
            Apply();
        }

        void Apply() { var (p, r) = Pose(target, yawDeg, pitchDeg, distance); transform.SetPositionAndRotation(p, r); }
    }
}
```

- [ ] **Step 4: MapRuntime 연결** — 프로퍼티 `public OrbitCamera Orbit { get; private set; }` 추가. `Awake` 의 `if (cam == null) cam = Camera.main; Placer.cam = cam;` 뒤에 `Orbit = cam.GetComponent<OrbitCamera>(); if (!Orbit) Orbit = cam.gameObject.AddComponent<OrbitCamera>();`. `Highlight` 끝에 `if (_selected != null && Orbit) Orbit.Focus(_markers[_selected].transform.position, 12f);`. `SetMode` 를

```csharp
        public void SetMode(string mode)
        {
            _mode = mode; Placer.enabledForInput = mode == "edit";
            if (mode == "edit") { Vehicle.running = false; if (Orbit) Orbit.follow = null; }
        }
```
로, `StartScenario` 의 `Vehicle.StartLane(lane, deck.z_surface);` 뒤에 `if (Orbit) { Orbit.follow = Vehicle.transform; Orbit.distance = 25f; Orbit.pitchDeg = 35f; }`.

- [ ] **Step 5: 통과 확인** — EditMode 배치 → `total="45" passed="45"`(DemoSceneTests 는 그대로 통과해야 한다 — 씬 파일은 건드리지 않는다).

- [ ] **Step 6: 커밋** — `git add unity/Assets/ShipHdMap && git commit -m "unity: orbit camera (right-drag/middle-drag/wheel), focus on selection, follow vehicle in drive mode"`

---

### Task 5: Unity — 차로·구획 라인 오버레이

**Files:**
- Create: `unity/Assets/ShipHdMap/Runtime/Ship/MapOverlay.cs`
- Modify: `MapRuntime.cs`, `unity/Assets/ShipHdMap/Editor/WebGLBuild.cs`
- Test: `MapRuntimeTests.cs`

**Interfaces:**
- `MapOverlay.Build(VehicleMap map, Transform parent) → GameObject "Overlay"`: 갑판별 자식(`Overlay/D3/…`) 아래 LineRenderer 하나씩 — 차로 `centerline` 노랑 폭 0.15, 구획 `polygon` 초록 폭 0.1, 표면 +0.05 m. 재질 `Unlit/Color`(색별 공유)
- `MapOverlay.SetDeck(GameObject overlay, string deckOrAll)`: 다른 갑판의 LineRenderer `enabled=false`
- `MapRuntime`: `Load` 가 오버레이를 재생성하고 현재 갑판 필터를 적용, `SetDeck` 이 필터를 기억(`_deck`)하고 오버레이에도 적용
- `WebGLBuild`: `IncludeShader("Unlit/Color")`

- [ ] **Step 1: 실패하는 테스트** — `MapRuntimeTests.cs` 에 추가:

```csharp
        [Test]
        public void LoadBuildsOverlayLinesAndSetDeckFilters()
        {
            go = new GameObject("Map"); var rt = go.AddComponent<MapRuntime>(); rt.InitForTest(); rt.Load(Fixture());
            var overlay = rt.transform.Find("Overlay"); Assert.That(overlay, Is.Not.Null);
            var lines = overlay.GetComponentsInChildren<LineRenderer>(true);
            Assert.That(lines.Length, Is.EqualTo(5)); // 3 lanes + 2 slots
            rt.SetDeck("D3");
            Assert.That(overlay.Find("D1/A2-D1-0001").GetComponent<LineRenderer>().enabled, Is.False);
            Assert.That(overlay.Find("D3/A2-D3-0001").GetComponent<LineRenderer>().enabled, Is.True);
            rt.Load(Fixture()); // rebuild keeps the filter and does not duplicate
            Assert.That(rt.transform.Find("Overlay").GetComponentsInChildren<LineRenderer>(true).Length, Is.EqualTo(5));
            Assert.That(rt.transform.Find("Overlay/D1/A2-D1-0001").GetComponent<LineRenderer>().enabled, Is.False);
        }
```

- [ ] **Step 2: 실패 확인** — EditMode 배치 → 이 테스트가 `overlay` null 로 실패.

- [ ] **Step 3: MapOverlay.cs**

```csharp
using System.Collections.Generic;
using UnityEngine;

namespace ShipHdMap
{
    /// Lane centerlines and parking-slot outlines as LineRenderers, grouped per deck so SetDeck can hide other decks.
    public static class MapOverlay
    {
        const float Lift = 0.05f, LaneWidth = 0.15f, SlotWidth = 0.1f;
        static readonly Color LaneColor = new Color(1f, 0.85f, 0.2f), SlotColor = new Color(0.2f, 0.9f, 0.4f);

        public static GameObject Build(VehicleMap map, Transform parent)
        {
            var root = new GameObject("Overlay"); root.transform.SetParent(parent, false);
            var mats = new Dictionary<Color, Material>();
            foreach (var lane in map.lanes ?? new List<Lane>()) Line(root, lane.deck_id, lane.id, lane.centerline, LaneColor, LaneWidth, mats);
            foreach (var slot in map.parking_slots ?? new List<ParkingSlot>()) Line(root, slot.deck_id, slot.id, slot.polygon, SlotColor, SlotWidth, mats);
            return root;
        }

        public static void SetDeck(GameObject overlay, string deckOrAll)
        {
            foreach (Transform deck in overlay.transform)
                foreach (var lr in deck.GetComponentsInChildren<LineRenderer>(true)) lr.enabled = deckOrAll == "all" || deck.name == deckOrAll;
        }

        static void Line(GameObject root, string deckId, string id, double[][] pts, Color c, float width, Dictionary<Color, Material> mats)
        {
            if (pts == null || pts.Length < 2) return;
            var deck = root.transform.Find(deckId ?? "none");
            if (deck == null) { deck = new GameObject(deckId ?? "none").transform; deck.SetParent(root.transform, false); }
            var g = new GameObject(id); g.transform.SetParent(deck, false);
            var lr = g.AddComponent<LineRenderer>();
            lr.useWorldSpace = true; lr.loop = false; lr.startWidth = lr.endWidth = width; lr.positionCount = pts.Length; // polygons already repeat their first point
            for (int i = 0; i < pts.Length; i++) lr.SetPosition(i, ShipFrame.ToUnity(pts[i][0], pts[i][1], pts[i][2] + Lift));
            if (!mats.TryGetValue(c, out var m)) { m = new Material(Shader.Find("Unlit/Color")) { color = c }; mats[c] = m; }
            lr.sharedMaterial = m; lr.shadowCastingMode = UnityEngine.Rendering.ShadowCastingMode.Off; lr.receiveShadows = false;
        }
    }
}
```

- [ ] **Step 4: MapRuntime** — 필드 `GameObject _overlay; string _deck = "all";` 추가. `Load` 끝(`Placer.nextId = …;` 뒤)에

```csharp
            if (_overlay) DestroyImmediate(_overlay);
            _overlay = MapOverlay.Build(CurrentMap, transform); MapOverlay.SetDeck(_overlay, _deck);
```
`SetDeck` 을 `public void SetDeck(string deck) { _deck = deck; if (Ship) ShipMeshBuilder.SetDeckVisibility(Ship, deck); if (_overlay) MapOverlay.SetDeck(_overlay, deck); }` 로.

- [ ] **Step 5: WebGLBuild** — `IncludeShader("Standard"); IncludeShader("Unlit/Texture");` 줄에 `IncludeShader("Unlit/Color");` 를 더한다(주석: halo·overlay 가 런타임에 `Shader.Find`).

- [ ] **Step 6: 통과 확인** — EditMode 배치 → `total="46" passed="46"`.

- [ ] **Step 7: 커밋** — `git add unity/Assets/ShipHdMap && git commit -m "unity: lane and slot line overlay per deck, hidden with SetDeck; include Unlit/Color in WebGL"`

---

### Task 6: Unity — UI Toolkit HUD(갑판·커서·선택·위치 추정)와 갑판 라벨

**Files:**
- Create: `unity/Assets/ShipHdMap/Editor/HudAssets.cs`, `unity/Assets/ShipHdMap/Runtime/Vehicle/HudView.cs`
- Create (생성 후 커밋): `unity/Assets/ShipHdMap/Resources/HudTheme.tss`, `unity/Assets/ShipHdMap/Resources/HudPanelSettings.asset` (+ `.meta`)
- Delete: `unity/Assets/ShipHdMap/Runtime/Vehicle/LocalizationHud.cs` (+ `.meta`)
- Modify: `MapRuntime.cs`
- Test: `unity/Assets/ShipHdMap/Tests/EditMode/HudViewTests.cs`(신규)

**Interfaces:**
- 에디터 메뉴 `ShipHdMap/Create HUD Assets`(`ShipHdMap.Editor.HudAssets.Create`, 배치 가능): `Resources/HudTheme.tss`(`@import url("unity-theme://default");`)와 `Resources/HudPanelSettings.asset`(테마 연결, `ConstantPixelSize`) 생성. 멱등
- `HudView`(MonoBehaviour, `Map` 에 부착): `Set(LocalizerResult, Pose2D truth, string frame)`(기존 HUD 와 같은 시그니처), `SetContext(string deck, string selected)`, `SetDeckLabels(IEnumerable<(string text, Vector3 world)>)`, `public Camera cam`. `static string[] Lines(deck, selected, cursor?, r, truth, frame)` 순수 함수. `OnEnable` 이 `Resources.Load<PanelSettings>("HudPanelSettings")` 로 `UIDocument` 를 구성하고, 없으면 경고 후 비활성
- 좌하단 반투명 박스: `deck D3   sel LM-0001` / `cursor x  48.09  y   6.81  z  11.67`(마우스 아래 선체 표면의 Ship Frame 좌표) / 주행 중이면 frame·N·RMS·est·true·err 4줄. 갑판 라벨은 각 갑판 선미 좌현 모서리 위 1.5 m 지점을 매 프레임 화면에 투영
- 모든 요소 `pickingMode = Ignore`(클릭이 씬으로 간다)
- `MapRuntime.Hud` 타입이 `HudView` 로 바뀐다. `SetDeck`/`Highlight` 가 `Hud.SetContext(_deck, _selected)`, `Load` 뒤에 라벨 갱신

- [ ] **Step 1: 실패하는 테스트** — `HudViewTests.cs`:

```csharp
using NUnit.Framework;
using UnityEngine;
using UnityEngine.UIElements;

namespace ShipHdMap.Tests
{
    public class HudViewTests
    {
        [Test]
        public void LinesFormatContextCursorAndEstimate()
        {
            var idle = HudView.Lines("D3", "LM-0001", (48.094, 6.81, 11.67), null, default, "SHIP_AP");
            Assert.That(idle.Length, Is.EqualTo(2));
            Assert.That(idle[0], Is.EqualTo("deck D3   sel LM-0001"));
            Assert.That(idle[1], Does.StartWith("cursor x   48.09"));
            var r = new LocalizerResult { ok = true, pose = new Pose2D { x = 1.85, y = -0.14, psiRad = 0.8 * System.Math.PI / 180 }, nObs = 2, residualRms = 0.031, iterations = 3 };
            var drive = HudView.Lines("all", null, null, r, new Pose2D { x = 2, y = 0, psiRad = 0 }, "SHIP_AP");
            Assert.That(drive.Length, Is.EqualTo(6));
            Assert.That(drive[1], Is.EqualTo("cursor -"));
            Assert.That(drive[2], Does.Contain("N 2")); Assert.That(drive[2], Does.Contain("RMS 0.031"));
            Assert.That(drive[5], Does.StartWith("err  0.21 m"));
        }

        [Test]
        public void HudAssetsExistInResources()
        {
            var ps = Resources.Load<PanelSettings>("HudPanelSettings");
            Assert.That(ps, Is.Not.Null, "run menu ShipHdMap/Create HUD Assets");
            Assert.That(ps.themeStyleSheet, Is.Not.Null);
        }
    }
}
```

- [ ] **Step 2: 실패 확인** — EditMode 배치 → `CS0246 'HudView'`.

- [ ] **Step 3: HudAssets.cs**

```csharp
using System.IO;
using UnityEditor;
using UnityEngine;
using UnityEngine.UIElements;

namespace ShipHdMap.Editor
{
    /// Creates the runtime UI Toolkit assets the HUD needs (a PanelSettings with the default theme) under Resources/. Idempotent.
    public static class HudAssets
    {
        const string Dir = "Assets/ShipHdMap/Resources", Tss = Dir + "/HudTheme.tss", Ps = Dir + "/HudPanelSettings.asset";

        [MenuItem("ShipHdMap/Create HUD Assets")]
        public static void Create()
        {
            if (!AssetDatabase.IsValidFolder(Dir)) AssetDatabase.CreateFolder("Assets/ShipHdMap", "Resources");
            if (!File.Exists(Tss)) { File.WriteAllText(Tss, "@import url(\"unity-theme://default\");\n"); AssetDatabase.ImportAsset(Tss); }
            var theme = AssetDatabase.LoadAssetAtPath<ThemeStyleSheet>(Tss);
            var ps = AssetDatabase.LoadAssetAtPath<PanelSettings>(Ps);
            if (ps == null) { ps = ScriptableObject.CreateInstance<PanelSettings>(); AssetDatabase.CreateAsset(ps, Ps); }
            ps.themeStyleSheet = theme; ps.scaleMode = PanelScaleMode.ConstantPixelSize; ps.scale = 1f;
            EditorUtility.SetDirty(ps); AssetDatabase.SaveAssets();
            Debug.Log($"[HudAssets] {Ps} theme={(theme ? theme.name : "null")}");
        }
    }
}
```

- [ ] **Step 4: 에셋 생성** — 에디터 GUI 닫힌 상태에서

```bash
cd unity && "$U" -batchmode -nographics -projectPath "$PWD" -executeMethod ShipHdMap.Editor.HudAssets.Create -quit -logFile "$PWD/Logs/hud-assets.log"; echo EXIT=$?
grep "HudAssets" Logs/hud-assets.log; ls Assets/ShipHdMap/Resources/
```
Expected: `[HudAssets] … theme=HudTheme`, 폴더에 `HudTheme.tss`, `HudPanelSettings.asset` 과 `.meta` 들. `theme=null` 이면 `AssetDatabase.Refresh()` 를 `ImportAsset` 뒤에 넣고 재실행; 그래도 null 이면 에디터 GUI 를 한 번 열어 메뉴를 실행하고 닫은 뒤 커밋한다(보고서에 기록).

- [ ] **Step 5: HudView.cs**

```csharp
using System;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UIElements;

namespace ShipHdMap
{
    /// UI Toolkit HUD: bottom-left status box (deck, selection, cursor in Ship Frame, localization while driving) and
    /// floating deck labels projected from world points. Every element ignores picking so scene clicks pass through.
    public class HudView : MonoBehaviour
    {
        public Camera cam;
        UIDocument _doc; Label _info; VisualElement _labelLayer;
        readonly List<(Label label, Vector3 world)> _deckLabels = new();
        LocalizerResult _last; Pose2D _truth; string _frame = "SHIP_AP", _deck = "all", _selected;

        public void Set(LocalizerResult r, Pose2D truth, string frame) { _last = r; _truth = truth; _frame = frame; }
        public void SetContext(string deck, string selected) { _deck = deck ?? "all"; _selected = selected; }

        public void SetDeckLabels(IEnumerable<(string text, Vector3 world)> items)
        {
            foreach (var (label, _) in _deckLabels) label.RemoveFromHierarchy();
            _deckLabels.Clear();
            if (_labelLayer == null) return;
            foreach (var (text, world) in items)
            {
                var l = new Label(text) { pickingMode = PickingMode.Ignore };
                Style(l.style, 12); l.style.position = Position.Absolute;
                _labelLayer.Add(l); _deckLabels.Add((l, world));
            }
        }

        public static string[] Lines(string deck, string selected, (double x, double y, double z)? cursor, LocalizerResult r, Pose2D truth, string frame)
        {
            const double R2D = 180 / Math.PI;
            var lines = new List<string>
            {
                $"deck {deck}   sel {selected ?? "-"}",
                cursor.HasValue ? $"cursor x {cursor.Value.x,7:F2}  y {cursor.Value.y,7:F2}  z {cursor.Value.z,6:F2}" : "cursor -",
            };
            if (r != null)
            {
                var e = r.pose; double err = Math.Sqrt((e.x - truth.x) * (e.x - truth.x) + (e.y - truth.y) * (e.y - truth.y));
                lines.Add($"frame {frame}   N {r.nObs}   RMS {r.residualRms:F3}   iter {r.iterations}{(r.ok ? "" : "   (holding previous)")}");
                lines.Add($"est  x {e.x,7:F2}  y {e.y,7:F2}  psi {e.psiRad * R2D,7:F1}");
                lines.Add($"true x {truth.x,7:F2}  y {truth.y,7:F2}  psi {truth.psiRad * R2D,7:F1}");
                lines.Add($"err  {err:F2} m   {ShipFrame.WrapDeg((e.psiRad - truth.psiRad) * R2D):F1} deg");
            }
            return lines.ToArray();
        }

        void OnEnable()
        {
            var ps = Resources.Load<PanelSettings>("HudPanelSettings");
            if (ps == null) { Debug.LogWarning("HudView: Resources/HudPanelSettings missing — run menu ShipHdMap/Create HUD Assets"); enabled = false; return; }
            _doc = GetComponent<UIDocument>(); if (_doc == null) _doc = gameObject.AddComponent<UIDocument>();
            _doc.panelSettings = ps;
            var root = _doc.rootVisualElement; root.pickingMode = PickingMode.Ignore;
            _labelLayer = new VisualElement { pickingMode = PickingMode.Ignore };
            _labelLayer.style.position = Position.Absolute; _labelLayer.style.left = 0; _labelLayer.style.top = 0; _labelLayer.style.right = 0; _labelLayer.style.bottom = 0;
            root.Add(_labelLayer);
            _info = new Label { pickingMode = PickingMode.Ignore };
            Style(_info.style, 12); _info.style.position = Position.Absolute; _info.style.left = 10; _info.style.bottom = 10; _info.style.whiteSpace = WhiteSpace.Normal;
            root.Add(_info);
        }

        static void Style(IStyle s, int fontSize)
        {
            s.backgroundColor = new Color(0, 0, 0, 0.55f); s.color = Color.white; s.fontSize = fontSize;
            s.paddingLeft = 6; s.paddingRight = 6; s.paddingTop = 3; s.paddingBottom = 3;
        }

        void LateUpdate()
        {
            if (_info == null) return;
            _info.text = string.Join("\n", Lines(_deck, _selected, CursorShip(), _last, _truth, _frame));
            if (cam == null) return;
            var panel = _doc.rootVisualElement.panel;
            foreach (var (label, world) in _deckLabels)
            {
                var vp = cam.WorldToViewportPoint(world);
                label.visible = vp.z > 0;
                if (!label.visible) continue;
                var p = RuntimePanelUtils.CameraTransformWorldToPanel(panel, world, cam);
                label.style.left = p.x; label.style.top = p.y;
            }
        }

        (double x, double y, double z)? CursorShip()
        {
            if (cam == null || !Application.isPlaying) return null;
            if (!Physics.Raycast(cam.ScreenPointToRay(Input.mousePosition), out var hit, 500f, LandmarkPlacer.StructureMask())) return null;
            return ShipFrame.ToShip(hit.point);
        }
    }
}
```

- [ ] **Step 6: LocalizationHud 삭제, MapRuntime 연결** — `git rm unity/Assets/ShipHdMap/Runtime/Vehicle/LocalizationHud.cs unity/Assets/ShipHdMap/Runtime/Vehicle/LocalizationHud.cs.meta`. `grep -rn LocalizationHud unity/Assets` 가 비어야 한다. `MapRuntime.cs`: 프로퍼티를 `public HudView Hud { get; private set; }`, `InitForTest` 의 `Hud = gameObject.AddComponent<LocalizationHud>();` → `Hud = gameObject.AddComponent<HudView>();`, `Awake` 의 `Placer.cam = cam;` 뒤에 `Hud.cam = cam;`. `Highlight` 끝과 `SetDeck` 끝에 `Hud.SetContext(_deck, _selected);`. `Load` 끝(오버레이 생성 뒤)에

```csharp
            var labels = new List<(string, Vector3)>();
            foreach (var d in CurrentMap.decks ?? new List<Deck>()) labels.Add(($"{d.id}  z {d.z_surface:F1} m", ShipFrame.ToUnity(4, 10, d.z_surface + 1.5)));
            Hud.SetDeckLabels(labels);
```
(`Update` 의 `Hud.Set(res, Vehicle.Truth, "SHIP_AP");` 는 그대로.)

- [ ] **Step 7: 통과 확인** — EditMode 배치 → `total="48" passed="48"`. `HudAssetsExistInResources` 가 실패하면 Step 4 로 돌아간다.

- [ ] **Step 8: 커밋** — `git add -A unity/Assets/ShipHdMap && git status --short unity | grep -v "^A\|^M\|^D" ; git commit -m "unity: UI Toolkit HUD (deck, cursor, selection, localization) with deck labels; drop IMGUI HUD"` — `unity/Logs/` 는 gitignore 라 올라가지 않는다.

---

### Task 7: 웹 — `onFeatureMoved` 저장, 초안 `mounted_on`, 우클릭 메뉴 차단

**Files:**
- Modify: `web/src/api/types.ts`, `web/src/store/editor.ts`, `web/src/bridge/useShipUnity.ts`, `web/src/components/PropertyForm.tsx`, `web/src/App.tsx`
- Test: `web/src/store/editor.test.ts` (2개 추가)

**Interfaces:**
- `FeatureMovedEvt = { id; x; y; z; normal: number[]; deck; mounted_on }`, `FeatureCreatedEvt.mounted_on?: string`
- 스토어 `moveFeature(e: FeatureMovedEvt): Promise<void>`: 저장된 피처면 `updateFeature(id, { deck_id, geometry Point, props: {...기존, normal, mounted_on} })`; 초안이면 초안의 geometry·deck_id 만 갱신(저장은 폼 `적용` 때)
- 훅: `onFeatureMoved` → `moveFeature(JSON.parse(json))`, 실패는 스토어 `error`
- `addDraft` 가 `mounted_on` 을 초안 props 에 넣고 폼이 그 값을 보여준다

- [ ] **Step 1: 실패하는 테스트** — `editor.test.ts` 에 추가:

```ts
  it("moveFeature PUTs geometry, deck and normal for a saved feature", async () => {
    await useEditorStore.getState().load("ds1");
    await useEditorStore.getState().moveFeature({ id: "LM-0001", x: 30, y: 6, z: 11.8, normal: [0, -1, 0], deck: "D3", mounted_on: "C-PILLAR-D3-009" });
    expect(api.updateFeature).toHaveBeenCalledWith("ds1", "LM-0001", { deck_id: "D3", geometry: { type: "Point", coordinates: [30, 6, 11.8] }, props: { code: 1, normal: [0, -1, 0], mounted_on: "C-PILLAR-D3-009" } });
    expect(useEditorStore.getState().features["LM-0001"].geometry.coordinates).toEqual([30, 6, 11.8]);
  });

  it("moveFeature on a draft only updates the draft", async () => {
    await useEditorStore.getState().load("ds1");
    useEditorStore.getState().addDraft({ tempId: "LM-0002", layer: "LM", x: 84, y: -6.2, z: 11.8, deck: "D3", mounted_on: "C-PILLAR-D3-001" });
    vi.mocked(api.updateFeature).mockClear();
    await useEditorStore.getState().moveFeature({ id: "LM-0002", x: 85, y: -6.2, z: 11.8, normal: [0, 1, 0], deck: "D3", mounted_on: "C-PILLAR-D3-002" });
    expect(api.updateFeature).not.toHaveBeenCalled();
    const d = useEditorStore.getState().drafts["LM-0002"];
    expect(d.geometry.coordinates).toEqual([85, -6.2, 11.8]);
    expect(d.props.mounted_on).toBe("C-PILLAR-D3-002");
  });
```

- [ ] **Step 2: 실패 확인** — `pnpm test` → `moveFeature is not a function`.

- [ ] **Step 3: 타입** — `types.ts` 의 `FeatureCreatedEvt` 에 `mounted_on?: string` 추가, 그 아래 `export type FeatureMovedEvt = { id: string; x: number; y: number; z: number; normal: number[]; deck: string; mounted_on: string };`.

- [ ] **Step 4: 스토어** — import 에 `FeatureMovedEvt` 추가; `EditorState` 에 `moveFeature: (e: FeatureMovedEvt) => Promise<void>;`; `addDraft` 의 `props: {}` 를 `props: { mounted_on: e.mounted_on ?? "" }` 로; `removeFeature` 뒤에

```ts
  async moveFeature(e) {
    const d = get().drafts[e.id];
    if (d) { set((s) => ({ drafts: { ...s.drafts, [e.id]: { ...d, deck_id: e.deck, geometry: { type: "Point", coordinates: [e.x, e.y, e.z] }, props: { ...d.props, normal: e.normal, mounted_on: e.mounted_on } } } })); return; }
    const f = get().features[e.id]; if (!f) return;
    await get().updateFeature(e.id, { deck_id: e.deck, geometry: { type: "Point", coordinates: [e.x, e.y, e.z] }, props: { ...f.props, normal: e.normal, mounted_on: e.mounted_on } });
  },
```

- [ ] **Step 5: 훅** — `useShipUnity.ts` 스토어 구조분해에 `moveFeature` 추가; 이벤트 이펙트에 `const onMoved = (json: string) => { void moveFeature(JSON.parse(json)).catch((e) => useEditorStore.setState({ error: "move failed: " + (e as Error).message })); };` 와 `addEventListener("onFeatureMoved", onMoved);`/`removeEventListener("onFeatureMoved", onMoved);` 추가, deps 에 `moveFeature`.

- [ ] **Step 6: 폼과 App** — `PropertyForm.tsx` 초안 분기의 `mounted_on: ""` 을 `mounted_on: (draft.props.mounted_on as string) ?? ""` 로. `App.tsx` 의 `<main className="center">` 를 `<main className="center" onContextMenu={(e) => e.preventDefault()}>` 로(우클릭 궤도 회전 시 브라우저 메뉴 차단).

- [ ] **Step 7: 통과 확인** — `pnpm test`(18/18) `pnpm build`(클린).

- [ ] **Step 8: 커밋** — `git add web/src && git commit -m "web: persist onFeatureMoved via PUT, draft mounted_on from Unity, block context menu over the scene"`

---

### Task 8: WebGL 재빌드, 브라우저 검증, README

**Files:**
- Modify: `README.md`
- 산출물: `web/public/unity/`(gitignore)

**Interfaces:**
- 구현자는 EditMode 48/48 확인 → WebGL 배치 빌드 → `./scripts/m3-dev.sh` 로 띄우고 `curl` 로 첫 화면(제목·데이터셋·loader 200)만 확인 → 종료. 브라우저 체크리스트는 컨트롤러가 Chrome 으로 수행(탭을 화면에 보이게 둘 것 — 숨은 탭은 rAF 가 멈춘다)

- [ ] **Step 1: 테스트와 빌드**

```bash
cd unity && "$U" -batchmode -nographics -projectPath "$PWD" -runTests -testPlatform EditMode -testResults "$PWD/Logs/editmode-results.xml" -logFile "$PWD/Logs/editmode.log"; grep -o 'total="[0-9]*" passed="[0-9]*" failed="[0-9]*"' Logs/editmode-results.xml
"$U" -batchmode -nographics -projectPath "$PWD" -executeMethod ShipHdMap.Editor.WebGLBuild.Build -quit -logFile "$PWD/Logs/webgl-build.log"; grep -E "\[WebGLBuild\]" Logs/webgl-build.log
cd ../web && pnpm test && pnpm build
```
Expected: `total="48" passed="48"`, `[WebGLBuild] added always-included shader: Unlit/Color`(첫 빌드) 와 `Succeeded`, Vitest 18, build 클린.

- [ ] **Step 2: 기동 확인** — `./scripts/m3-dev.sh &` → 20초 후 `curl -s localhost:5173/ | grep -o "<title>[^<]*"`(5173 이 막혀 있으면 스크립트 출력의 포트) → `Ship HD Map Editor`; 종료 후 `pgrep -f ApiApplication || echo stopped`, `pgrep -f vite || echo stopped`.

- [ ] **Step 3: README** — `## 상태` 의 `- M3a 웹 편집기 완료 (…)` 다음 줄 `- 다음: M3b Unity 씬 UX` 를 `- M3b Unity 씬 UX 완료 (궤도 카메라, 클릭 선택·드래그 이동 → DB, 차로·구획 라인, UI Toolkit HUD; EditMode 48, Vitest 18)` 와 `- 다음: M4 적재 계획` 두 줄로 바꾼다. `## 실행` 의 준비 줄에 `Unity 6.3(WebGL 빌드 1회: 메뉴 \`ShipHdMap/Build WebGL\`)` 뒤에 `, HUD 에셋은 저장소에 포함` 을 덧붙인다.

- [ ] **Step 4: 브라우저 체크리스트** (컨트롤러가 Chrome 으로; 구현자는 "미수행 (컨트롤러 수행)")

1. 첫 화면: 차로가 노란 선, 구획이 초록 선으로 갑판 위에 보인다. 좌하단 HUD 에 `deck all   sel -` 와 `cursor …`(마우스를 선체 위로 올리면 좌표가 바뀐다). 갑판 라벨 `D1 z 5.4 m`… 가 각 갑판 선미 좌현에 떠 있다. 콘솔 오류 0
2. 우클릭 드래그로 회전, 휠로 줌, 가운데 드래그로 팬. 우클릭 시 브라우저 메뉴가 뜨지 않는다
3. 트리에서 `LM-0001` 클릭 → 3D 에서 노란 halo, 카메라가 마커로 이동, 미니맵 파란 표시, HUD `sel LM-0001`, 상태바 `선택 LM-0001`
4. 3D 에서 다른 마커 클릭 → 트리·미니맵·상태바·HUD 가 그 id 로 바뀐다(왕복 루프 없음: 콘솔에 반복 이벤트 없음)
5. 마커를 옆 기둥으로 드래그해 놓기 → 속성 폼 위치가 바뀌고 `curl localhost:8081/api/datasets/roro-demo-01/features/LM-0001` 의 좌표·`props.normal`·`props.mounted_on` 이 바뀌며 상단 version +1, 미니맵 점이 이동
6. 기둥 클릭 → 초안 props 의 `mounted_on` 이 기둥 id 로 채워져 있다 → `적용(저장)` → DB `props.mounted_on` 확인 → `삭제`
7. `D3` 탭 → D1·D2 의 라인이 사라지고 D3 만 남는다; `전체` 로 복귀
8. `주행` → `▶ 선적 시나리오` → 카메라가 차량을 따라가고 HUD 에 est/true/err 4줄이 갱신된다 → `정지` → 편집 탭, 카메라 추적 해제(우클릭 회전이 다시 자유롭다)

- [ ] **Step 5: 커밋** — `git add README.md && git commit -m "docs: M3b status"`

---

## 자기 검토 메모

- 스펙 §13 M3b 행 대조: 궤도 카메라·차량 추적(T4), 마커 선택 하이라이트(T2)·드래그 이동 `onFeatureMoved`(T3+T7), 차로·구획 라인(T5), 갑판 라벨(T6, HUD 레이어에 투영), HUD 를 UI Toolkit 으로(T6), WebGL 재빌드(T8). 완료 기준 "드래그하면 DB 좌표가 바뀜"(T7+체크 5), "선택이 3D·트리·미니맵에서 일치"(T2+T7+체크 3·4)
- 이름 일관성: `LandmarkPlacer.Selected/Moved`, `LandmarkMarker.SetHighlighted/MoveTo`, `MapRuntime.Highlight/OnMarkerMoved/Orbit/_deck/_selected`, `MapOverlay.Build/SetDeck`, `HudView.Set/SetContext/SetDeckLabels/Lines`, `FeatureMovedEvt` 필드가 C#(`BridgeMessages.cs`)·TS(`types.ts`)·스펙 §10·계약 문서에서 동일
- 테스트 수: Unity 40 → T2 +2 → T3 +2 → T4 +1 → T5 +1 → T6 +2 = 48; web 15 → T1 +1 → T7 +2 = 18
- 알려진 위험: (1) UI Toolkit 에셋을 배치 모드로 만들 때 `.tss` 임포트가 늦을 수 있다 → T6 Step 4 의 재시도·GUI 대안. (2) 브라우저에서 가운데 버튼 드래그는 일부 OS 에서 자동 스크롤을 켠다 → 체크 2 에서 확인하고 문제면 Shift+우클릭 팬으로 대체(구현자 재량 아님, 컨트롤러 판정). (3) `Input.GetAxis("Mouse X")` 는 레거시 Input Manager(`activeInputHandler: 0`) 전제 — 유지
